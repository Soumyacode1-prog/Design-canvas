# Editor behavior

Shapes are kept in React state and rendered by React Konva. Dragging and property edits update the selected element. Saving sends the scene to the authenticated canvas API; PNG export captures the current Konva stage at 2x pixel ratio.
