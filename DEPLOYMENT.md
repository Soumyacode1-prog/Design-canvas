# Deployment

Deploy the frontend and backend separately or behind one HTTPS site. Set `CLIENT_URL` to the exact frontend origin, use `NODE_ENV=production`, and provide the MongoDB Atlas URI. Rebuild the frontend whenever `NEXT_PUBLIC_API_URL` changes.
